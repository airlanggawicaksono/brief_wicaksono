# Stub — wire langchain/langgraph/mcp here later.
#
# Example usage:
#   from langchain_core.messages import HumanMessage
#   from langgraph.graph import StateGraph
#
# This module is intentionally empty.
# The PredictService.predict() method should call into this
# once you have your LLM routing set up.
